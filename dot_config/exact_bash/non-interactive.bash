#!/usr/bin/env bash
# Sourced in every bash shell (interactive and non-interactive).
# Currently empty. Add content here only if it must run in every bash
# shell regardless of interactivity. Anything that produces output, calls
# `complete`, or otherwise assumes an interactive shell does not belong
# here -- put it in interactive.bash.
