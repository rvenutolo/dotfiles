# Kernel hardening. kptr_restrict is deliberately NOT set here: it is owned by
# sysctl-intellij-profiler.conf (kptr_restrict = 0), which the profiler needs.
# Keeping it out of this file avoids a filename-ordering dependency.
kernel.randomize_va_space = 2
kernel.dmesg_restrict = 1
